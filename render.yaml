services:
  - type: web
    name: dental-implant
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist -l 10000
    envVars:
      - key: NODE_VERSION
        value: 22
